framework:
    session:
        storage_factory_id: session.storage.factory.mock_file
    assets:

security:
    password_hashers:
        Sonata\UserBundle\Model\UserInterface:
            algorithm: plaintext

    firewalls:
        main:
            lazy: true
